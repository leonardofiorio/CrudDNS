<p id="notice"><%= notice %></p>

<div id="titulo">Domains</div>

<div>
  <p> 
    <h4>A tabela abaixo mostra os domains cadastrados no sistema.</h4>
    <h4>Clique em exibir no Domain desejado para inserir, alterar, remover e visualizar os Records.</h4>
  </p>
</div>


<%= link_to 'Inserir novo Domain', new_domain_path %>

</br>
</br>

<table id="domains" class=" datatable table table-striped table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Ttl</th>
      <th>Primary name server</th>
      <th>Contact</th>
      <th>Refresh</th>
      <th>Retry</th>
      <th>Expire</th>
      <th>Minimum</th>
      <th>Ações</th>
    </tr>
  </thead>

  <tbody>
    <% @domains.each do |domain| %>
      <tr>
        <td><%= domain.name %></td>
        <td><%= domain.type_domain %></td>
        <td><%= domain.ttl %></td>
        <td><%= domain.primary_name_server %></td>
        <td><%= domain.contact %></td>
        <td><%= domain.refresh %></td>
        <td><%= domain.retry %></td>
        <td><%= domain.expire %></td>
        <td><%= domain.minimum %></td>
        <td>
            <%= link_to 'Exibir', domain %>
            <%= link_to 'Editar', edit_domain_path(domain) %>
            <%= link_to 'Excluir', domain, method: :delete, data: { confirm: 'Tem certeza que deseja excluir este Domain?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


</br>

